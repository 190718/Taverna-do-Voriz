import 'package:flutter/material.dart';

class Conhecimento extends StatelessWidget {
  const Conhecimento({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
