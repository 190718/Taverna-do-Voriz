import 'package:flutter/material.dart';

class Sobrevivencia extends StatelessWidget {
  const Sobrevivencia({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
