import 'package:flutter/material.dart';

class Furtividade extends StatelessWidget {
  const Furtividade({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
