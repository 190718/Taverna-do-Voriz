import 'package:flutter/material.dart';

class Ladinagem extends StatelessWidget {
  const Ladinagem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
