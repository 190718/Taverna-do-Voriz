import 'package:flutter/material.dart';

class Vontade extends StatelessWidget {
  const Vontade({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
