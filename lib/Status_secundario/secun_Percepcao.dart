import 'package:flutter/material.dart';

class Percepcao extends StatelessWidget {
  const Percepcao({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
