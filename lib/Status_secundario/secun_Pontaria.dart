import 'package:flutter/material.dart';

class Pontaria extends StatelessWidget {
  const Pontaria({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
