import 'package:flutter/material.dart';

class Lideranca extends StatelessWidget {
  const Lideranca({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
